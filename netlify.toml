# Netlify config — deploys the static PWA in /frontend.
[build]
  publish = "frontend"
  command = ""

# SPA-style fallback (single page)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache"
